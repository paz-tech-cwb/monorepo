# Archive

This folder preserves old agent/planning/superpowers material that is no longer the canonical AI context.

Canonical context now lives in `.ai/`.

Rules:

- Use archived files for historical reference only.
- Do not treat archived plans/specs as current requirements if they conflict with `.ai/`.
- Promote still-relevant details into `.ai/features/*` or `.ai/apps/*` instead of linking agents directly to archived files.

Additional historical docs were copied to `docs/archive/` during the 2026-07-16 documentation restructure.
